# Original updated content for check_server.yml file
 # Existing logic here
- name: Retrieve online time from timeapi.io
  uri:
    url: "https://timeapi.io/api/Time/current/zone?timeZone=UTC"
    method: GET
 # Ensure content around remains same.
     filter_updates intact.